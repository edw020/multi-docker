language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t rok0sbasilisk/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true rok0sbasilisk/react-test npm test

after_success:
    - docker build -t rok0sbasilisk/multi-client ./client
    - docker build -t rok0sbasilisk/multi-nginx ./nginx
    - docker build -t rok0sbasilisk/multi-server ./server
    - docker build -l rok0sbasilisk/multi-worker ./worker
    # Log in to the docker CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to docker hub
    - docker push rok0sbasilisk/multi-client
    - docker push rok0sbasilisk/multi-nginx
    - docker push rok0sbasilisk/multi-server
    - docker push rok0sbasilisk/multi-worker